<ul class="unstyled">
  <% @categories.each do |category| %>
  <li>
    <h3><%= category %></h3>
    <ul class="unstyled">
      <% category.users.each do |user| %>
        <li><%= link_to user.name, user %></li>
      <% end %>
    </ul>
  </li>
  <% end %>
</ul>
